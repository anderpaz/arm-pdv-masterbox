#!/bin/bash
regedit="/root/.cxoffice/Aramo/user.reg"

#################################################################

run() {

	sed -i 's\"Wallpaper"=*.*\"Wallpaper"="c://wallpaper.bmp"\' $regedit

}
case $1 in

wall)
	run
	;;
esac
