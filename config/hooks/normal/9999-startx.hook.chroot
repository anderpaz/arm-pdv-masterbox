#!/bin/bash

set -e

if ! grep -q 'startx' /etc/profile; then
  echo '# Startx tty1' >>/etc/profile
  echo 'if [ -z "$DISPLAY" ] && [ "$(tty)" = "/dev/tty1" ]; then' >>/etc/profile
  echo '  startx' >>/etc/profile
  echo 'fi' >>/etc/profile
fi
